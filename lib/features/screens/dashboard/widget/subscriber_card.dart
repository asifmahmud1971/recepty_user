import 'package:flutter/cupertino.dart';

class SubscriberCard extends StatelessWidget {
  const SubscriberCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
