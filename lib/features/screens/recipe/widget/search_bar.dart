/*
import 'package:easy_localization/easy_localization.dart';
import 'package:flutter/material.dart';
import 'package:recepty/core/constants/app_size.dart';
import 'package:recepty/core/constants/app_strings.dart';

class SearchBar extends StatelessWidget {
  const SearchBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.symmetric(horizontal: 10.0),
      child: Container(
        decoration: BoxDecoration(
          border: Border.all(color: Colors.black12),
             borderRadius: BorderRadius.circular(10)),
        child: Row(
          children: [
            Padding(
              padding: const EdgeInsets.symmetric(horizontal: 10.0, vertical: 10),
              child: Icon(Icons.search_sharp,color: Colors.black26,),
            ),
            Expanded(child: Container(
              child: Text(AppStrings.searchHint.tr(),style: kRegularLine16.copyWith(color: Colors.black26),),
            ))
          ],
        ),
      ),
    );
  }
}
*/
